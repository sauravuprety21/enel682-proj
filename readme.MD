# Example workspace demonstraing data extraction from ROS2 bags using Python
`conda config --append channels conda-forge`
`conda create --name rosbag_env --file environment.yml`
`conda activate rosbag_env`
